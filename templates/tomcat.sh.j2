#!/bin/bash

pid=`ps -fe | grep '{{tomcat_dir}}/{{tomcat_version}}' | grep -v grep | tr -s ' ' | cut -d " " -f2`

if [ -n "$pid" ]; then 
	{{tomcat_dir}}/{{tomcat_version}}/bin/catalina.sh stop 10 -force
fi

{{tomcat_dir}}/{{tomcat_version}}/bin/startup.sh